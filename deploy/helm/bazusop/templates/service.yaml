apiVersion: v1
kind: Service
metadata:
  name: {{ include "bazusop.fullname" . }}
  labels:
    {{- include "bazusop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bazusop.selectorLabels" . | nindent 4 }}
